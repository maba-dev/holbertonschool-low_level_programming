#include <stdio.h>
/**
* main - entry point
* @argc: count argement
* @argv: parameter argument
* Return: int
*/

int	main(int argc, char **argv)
{
	if (argc == 1)
		printf("%s\n", argv[0]);
	return (0);
}
